


function setup() {
  createCanvas(500, 500);
  textAlign(CENTER);
  rectMode(CENTER);
}

function draw() {

  switch(state)

  case 0:
  background('red');
  text("Why did the chicken\n cross the road?", 250, 250);

  timer = timer + 1

}
